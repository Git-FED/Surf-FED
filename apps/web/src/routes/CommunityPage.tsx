import posts from '../data/community.json'; export default function CommunityPage(){return <><div className="eyebrow">THE BLOCK</div><h1>Build together.</h1><p className="lede">A place to share progress, ask for help, and find collaborators.</p><div className="grid">{posts.map(p=><article className="card" key={p.author}><div className="mono">{p.author}</div><p>{p.body}</p><div className="muted">♡ {p.likes} people resonated</div></article>)}</div></>}
