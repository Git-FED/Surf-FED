import items from '../data/blueprints.json'; export default function BlueprintPage(){return <><div className="eyebrow">BLUEPRINT LIBRARY</div><h1>Ideas with a next step.</h1><div className="grid">{items.map(i=><article className="card" key={i.title}><div className="mono">{i.category} · {i.status}</div><h3>{i.title}</h3><p className="muted">A starting point for turning intention into a useful project.</p></article>)}</div></>}
