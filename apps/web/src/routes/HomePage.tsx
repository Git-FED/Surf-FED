import home from '../data/home.json'; import courses from '../data/courses.json';
export default function HomePage(){return <><section className="hero"><div><div className="eyebrow">{home.eyebrow}</div><h1>{home.title}</h1><p className="lede">{home.description}</p><div className="actions"><a className="button primary" href="/courses">Start learning</a><a className="button" href="/community">Enter the community</a></div><div className="grid">{home.stats.map(s=><div className="card" key={s.label}><div className="mono">{s.value}</div><div className="muted">{s.label}</div></div>)}</div></div><div className="hero-art"><span>MUTE THE NOISE · BUILD YOUR SIGNAL</span></div></section><section><h2>Continue your path</h2><div className="grid">{courses.map(c=><article className="card" key={c.id}><div className="mono">{c.tag} · {c.progress}%</div><h3>{c.title}</h3><p className="muted">{c.description}</p></article>)}</div></section></>}
