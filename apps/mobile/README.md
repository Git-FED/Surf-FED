# FED-EDU Mobile

Capacitor packaging target for the shared `apps/web` build. Run `npm run build` in `apps/web`, then initialize native platforms with `npx cap add android` and `npx cap add ios` when toolchains and signing are configured.
