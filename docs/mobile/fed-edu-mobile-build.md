# FED-EDU mobile build

Build the web app from `apps/web`, then use Capacitor to sync and open native projects. Android requires Android Studio and signing credentials. iOS requires Xcode, an Apple developer account, provisioning, and signing.
